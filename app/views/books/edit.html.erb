<div class="containt">
  <h2>Editing Book</h2>
  <% if @book.errors.any? %>
    <div class="errors">
      <div class="error">
        <p><%= @book.errors.count %><span> errors prohibited this book from being saved:</span></p>
      </div>
      <ul class="ul">
        <% @book.errors.full_messages.each do |message| %>
          <li><span>・</span><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= form_with model: @book do |f| %>
    <P>Title</P>
    <%= f.text_field :title %>
    <P>Body</P>
    <%= f.text_area :body %>
    <br>
    <%= f.submit "Update Book" %>
    <br>
    <%= link_to "show", book_path(@book.id) %>
    <%= link_to "back", "/books" %>
  <% end %>
</div>